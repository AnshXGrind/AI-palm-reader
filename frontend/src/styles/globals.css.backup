/* Modern Dark Theme - Inspired by Saksham's Design */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
  background: #0a0a0a;
  background-image: 
    radial-gradient(circle at 20% 50%, #1a1a2e15 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, #16213e15 0%, transparent 50%),
    radial-gradient(circle at 40% 80%, #0f346015 0%, transparent 50%);
  min-height: 100vh;
  color: #e2e8f0;
  overflow-x: hidden;
  line-height: 1.6;
}

.container {
  max-width: 1400px;
  margin: 0 auto;
  padding: 1rem;
  position: relative;
}

/* Mobile-first responsive design */
@media (min-width: 640px) {
  .container {
    padding: 1.5rem;
  }
}

@media (min-width: 768px) {
  .container {
    padding: 2rem;
  }
}

@media (min-width: 1024px) {
  .container {
    padding: 2rem;
  }
}

/* Hero Section - Mobile First */
.hero {
  text-align: center;
  margin-bottom: 2rem;
  padding: 2rem 0;
  position: relative;
}

@media (min-width: 768px) {
  .hero {
    margin-bottom: 3rem;
    padding: 3rem 0;
  }
}

@media (min-width: 1024px) {
  .hero {
    margin-bottom: 4rem;
    padding: 4rem 0;
  }
}

.hero::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 300px;
  height: 300px;
  background: radial-gradient(circle, #3b82f650 0%, #8b5cf650 50%, transparent 70%);
  border-radius: 50%;
  z-index: -1;
  animation: pulse 4s ease-in-out infinite;
}

@media (min-width: 640px) {
  .hero::before {
    width: 400px;
    height: 400px;
  }
}

@media (min-width: 768px) {
  .hero::before {
    width: 500px;
    height: 500px;
  }
}

@media (min-width: 1024px) {
  .hero::before {
    width: 600px;
    height: 600px;
  }
}

@keyframes pulse {
  0%, 100% { opacity: 0.3; transform: translateX(-50%) scale(1); }
  50% { opacity: 0.6; transform: translateX(-50%) scale(1.1); }
}

.hero h1 {
  font-size: 2rem;
  font-weight: 800;
  line-height: 1.2;
}

@media (min-width: 640px) {
  .hero h1 {
    font-size: 2.5rem;
  }
}

@media (min-width: 768px) {
  .hero h1 {
    font-size: 3rem;
  }
}

@media (min-width: 1024px) {
  .hero h1 {
    font-size: 4rem;
  }
}

.hero h1 {
  margin-bottom: 1rem;
  background: linear-gradient(135deg, #3b82f6, #8b5cf6, #06b6d4);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-shadow: 0 0 30px rgba(59, 130, 246, 0.5);
}

@media (min-width: 768px) {
  .hero h1 {
    margin-bottom: 1.5rem;
  }
}

.hero p {
  font-size: 1.25rem;
  color: #94a3b8;
  max-width: 600px;
  margin: 0 auto;
  font-weight: 300;
}

/* Upload Section */
.upload-section {
  margin: 3rem 0;
}

.upload-area {
  background: rgba(15, 23, 42, 0.8);
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
  border: 2px dashed #334155;
  border-radius: 20px;
  padding: 3rem;
  text-align: center;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.upload-area::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.1), transparent);
  transition: left 0.5s ease;
}

.upload-area:hover::before {
  left: 100%;
}

.upload-area:hover {
  border-color: #3b82f6;
  background: rgba(15, 23, 42, 0.9);
  transform: translateY(-5px);
  box-shadow: 0 20px 40px rgba(59, 130, 246, 0.2);
}

.upload-area.dragover {
  border-color: #8b5cf6;
  background: rgba(139, 92, 246, 0.1);
  transform: scale(1.02);
}

.upload-input {
  display: none;
}

.upload-content {
  position: relative;
  z-index: 2;
}

.upload-icon {
  font-size: 2.5rem;
  margin-bottom: 1rem;
  display: block;
  opacity: 0.8;
}

@media (min-width: 640px) {
  .upload-icon {
    font-size: 3rem;
  }
}

@media (min-width: 768px) {
  .upload-icon {
    font-size: 4rem;
  }
}

.upload-content h3 {
  font-size: 1.25rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: #f1f5f9;
}

@media (min-width: 640px) {
  .upload-content h3 {
    font-size: 1.5rem;
  }
}

@media (min-width: 768px) {
  .upload-content h3 {
    font-size: 1.8rem;
  }
}

.upload-content p {
  color: #94a3b8;
  margin-bottom: 1.5rem;
  font-size: 0.95rem;
}

@media (min-width: 640px) {
  .upload-content p {
    font-size: 1rem;
    margin-bottom: 2rem;
  }
}

@media (min-width: 768px) {
  .upload-content p {
    font-size: 1.1rem;
  }
}

.supported-formats {
  margin: 1rem 0;
  padding: 0.5rem 1rem;
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.2);
  border-radius: 8px;
}

.supported-formats small {
  color: #10b981;
  font-weight: 500;
}

.upload-buttons {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
  flex-wrap: wrap;
}

@media (min-width: 640px) {
  .upload-buttons {
    gap: 1rem;
  }
}

.upload-button, .camera-button {
  background: linear-gradient(135deg, #3b82f6, #1d4ed8);
  color: white;
  border: none;
  padding: 0.875rem 1.5rem;
  border-radius: 50px;
  font-size: 0.875rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
  box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
  min-width: 120px;
  text-align: center;
}

@media (min-width: 640px) {
  .upload-button, .camera-button {
    padding: 1rem 1.75rem;
    font-size: 0.95rem;
    min-width: 140px;
  }
}

@media (min-width: 768px) {
  .upload-button, .camera-button {
    padding: 1rem 2rem;
    font-size: 1rem;
    min-width: 160px;
  }
}

.camera-button {
  background: linear-gradient(135deg, #8b5cf6, #7c3aed);
  box-shadow: 0 8px 25px rgba(139, 92, 246, 0.3);
}

.upload-button:hover, .camera-button:hover {
  transform: translateY(-3px);
  box-shadow: 0 15px 35px rgba(59, 130, 246, 0.4);
}

.camera-button:hover {
  box-shadow: 0 15px 35px rgba(139, 92, 246, 0.4);
}

/* Preview Section */
.preview-container {
  margin-top: 2rem;
  padding: 2rem;
  background: rgba(15, 23, 42, 0.6);
  border-radius: 15px;
  text-align: center;
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(59, 130, 246, 0.2);
}

.preview-container h4 {
  color: #10b981;
  font-size: 1.3rem;
  margin-bottom: 1rem;
  font-weight: 600;
}

.preview-image {
  max-width: 100%;
  height: auto;
  border-radius: 10px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  border: 2px solid rgba(59, 130, 246, 0.3);
}

.preview-image.small {
  max-height: 300px;
}

.preview-note {
  color: #10b981;
  font-style: italic;
  margin-top: 1rem;
  font-size: 0.95rem;
  text-align: center;
  opacity: 0.9;
}

.preview-section {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  align-items: start;
  margin-bottom: 2rem;
}

.result-section {
  margin: 3rem 0;
}

.result-container {
  background: rgba(15, 23, 42, 0.8);
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
  border-radius: 20px;
  padding: 2rem;
  border: 1px solid rgba(59, 130, 246, 0.2);
}

.result-header {
  text-align: center;
  margin-bottom: 2rem;
}

.result-header h2 {
  font-size: 2.5rem;
  font-weight: 700;
  background: linear-gradient(135deg, #3b82f6, #8b5cf6);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1rem;
}

.analysis-content {
  line-height: 1.8;
}

.reading-section {
  font-weight: 600;
  color: #3b82f6;
  margin: 1.5rem 0 0.5rem 0;
  font-size: 1.2rem;
}

.reading-text {
  margin: 0.5rem 0;
  color: #e2e8f0;
  font-size: 1rem;
}

/* Error States */
.error {
  background: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.3);
  border-radius: 15px;
  padding: 2rem;
  text-align: center;
  margin: 2rem 0;
}

.error h3 {
  color: #ef4444;
  margin-bottom: 1rem;
}

.error p {
  color: #fca5a5;
  margin-bottom: 1rem;
}

.loading {
  text-align: center;
  padding: 3rem;
}

.loading-spinner {
  display: inline-block;
  width: 50px;
  height: 50px;
  border: 3px solid rgba(59, 130, 246, 0.3);
  border-top: 3px solid #3b82f6;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 1rem;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading h3 {
  color: #f1f5f9;
  font-size: 1.5rem;
  margin-bottom: 0.5rem;
}

.loading p {
  color: #94a3b8;
}

.interpretation {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  color: white;
  padding: 2rem;
  border-radius: 15px;
  font-size: 1.1rem;
  line-height: 1.6;
  white-space: pre-wrap;
  box-shadow: 0 4px 20px rgba(240, 147, 251, 0.3);
}

.error {
  background: #fee;
  color: #c53030;
  padding: 1rem;
  border-radius: 10px;
  border: 1px solid #fed7d7;
}

/* Features Section - Mobile First */
.features {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1.5rem;
  margin: 2rem 0;
  padding: 0 0.5rem;
  width: 100%;
  max-width: 100%;
  box-sizing: border-box;
}

/* Tablet view - 2 columns */
@media (min-width: 640px) {
  .features {
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin: 3rem 0;
    padding: 0;
  }
}

/* Desktop view - 4 columns */
@media (min-width: 1024px) {
  .features {
    grid-template-columns: repeat(4, 1fr);
    margin: 4rem 0;
  }
}

.feature-card {
  background: rgba(15, 23, 42, 0.8);
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 15px;
  padding: 1.5rem;
  text-align: center;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
  width: 100%;
  max-width: 100%;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  min-height: 200px;
}

@media (min-width: 640px) {
  .feature-card {
    padding: 2rem;
    border-radius: 20px;
  }
}

.feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(59, 130, 246, 0.1), transparent);
  transition: left 0.5s ease;
}

.feature-card:hover::before {
  left: 100%;
}

.feature-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 25px 50px rgba(59, 130, 246, 0.2);
  border-color: #3b82f6;
}

@media (min-width: 768px) {
  .feature-card:hover {
    transform: translateY(-10px);
  }
}

.feature-icon {
  font-size: 2.5rem;
  margin-bottom: 0.75rem;
  opacity: 0.9;
}

@media (min-width: 640px) {
  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
}

.feature-card h3 {
  color: #f1f5f9;
  font-size: 1.25rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
  text-align: center;
  flex-shrink: 0;
}

@media (min-width: 640px) {
  .feature-card h3 {
    font-size: 1.4rem;
    margin-bottom: 1rem;
  }
}

.feature-card p {
  color: #94a3b8;
  line-height: 1.6;
  text-align: center;
  flex-grow: 1;
  font-size: 0.95rem;
}

@media (min-width: 640px) {
  .feature-card p {
    font-size: 1rem;
  }
}

/* Camera Overlay */
.camera-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.95);
  -webkit-backdrop-filter: blur(15px);
  backdrop-filter: blur(15px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
  animation: fadeIn 0.3s ease;
  padding: 1rem;
  box-sizing: border-box;
}

@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}

.camera-modal {
  background: rgba(15, 23, 42, 0.98);
  border-radius: 20px;
  padding: 1rem;
  max-width: 95vw;
  max-height: 95vh;
  border: 2px solid rgba(59, 130, 246, 0.5);
  box-shadow: 0 25px 50px rgba(59, 130, 246, 0.3);
  width: 100%;
  margin: 0 auto;
  overflow: hidden;
  position: relative;
}

@media (min-width: 640px) {
  .camera-modal {
    padding: 1.5rem;
    max-width: 90vw;
    max-height: 90vh;
    border-radius: 20px;
  }
}

@media (min-width: 768px) {
  .camera-modal {
    padding: 2rem;
    max-width: 80vw;
  }
}

.camera-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;
}

@media (min-width: 640px) {
  .camera-header {
    margin-bottom: 1.5rem;
  }
}

@media (min-width: 768px) {
  .camera-header {
    margin-bottom: 2rem;
  }
}

.camera-header h3 {
  color: #f1f5f9;
  font-size: 1.25rem;
  font-weight: 600;
}

@media (min-width: 640px) {
  .camera-header h3 {
    font-size: 1.375rem;
  }
}

@media (min-width: 768px) {
  .camera-header h3 {
    font-size: 1.5rem;
  }
}

.close-button {
  background: rgba(239, 68, 68, 0.8);
  color: white;
  border: none;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  font-size: 1.2rem;
  cursor: pointer;
  transition: all 0.3s ease;
}

.close-button:hover {
  background: #dc2626;
  transform: scale(1.1);
}

.camera-content {
  padding: 1.5rem;
}

.camera-error {
  text-align: center;
  padding: 2rem;
  background: rgba(239, 68, 68, 0.1);
  border-radius: 15px;
  border: 1px solid rgba(239, 68, 68, 0.3);
  margin: 1rem;
}

.camera-error .error-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.camera-error h4 {
  color: #ef4444;
  margin-bottom: 1rem;
  font-size: 1.25rem;
  font-weight: 600;
}

.camera-error p {
  color: #ef4444;
  margin-bottom: 1.5rem;
  line-height: 1.5;
}

.error-actions {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
}

.retry-button {
  background: linear-gradient(135deg, #10b981, #059669);
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  border-radius: 25px;
  cursor: pointer;
  font-weight: 600;
  font-size: 0.95rem;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(16, 185, 129, 0.3);
}

.retry-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(16, 185, 129, 0.4);
}

.error-button {
  background: linear-gradient(135deg, #6b7280, #4b5563);
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  border-radius: 25px;
  cursor: pointer;
  font-weight: 600;
  font-size: 0.95rem;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(107, 114, 128, 0.3);
}

.error-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(107, 114, 128, 0.4);
}

.emergency-button {
  background: linear-gradient(135deg, #f59e0b, #d97706);
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  border-radius: 25px;
  cursor: pointer;
  font-weight: 600;
  font-size: 0.95rem;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(245, 158, 11, 0.3);
}

.emergency-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(245, 158, 11, 0.4);
}

.timeout-warning {
  background: rgba(245, 158, 11, 0.1);
  border: 1px solid rgba(245, 158, 11, 0.3);
  border-radius: 10px;
  padding: 1rem;
  margin: 1rem 0;
  text-align: center;
}

.timeout-warning p {
  color: #f59e0b;
  margin-bottom: 1rem;
  font-size: 0.9rem;
}

.fallback-upload-button {
  background: linear-gradient(135deg, #3b82f6, #1d4ed8);
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  border-radius: 25px;
  cursor: pointer;
  font-weight: 600;
  font-size: 0.95rem;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
}

.fallback-upload-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(59, 130, 246, 0.4);
}

.camera-start {
  text-align: center;
  padding: 2rem;
}

.camera-icon {
  font-size: 4rem;
  margin-bottom: 1rem;
  opacity: 0.9;
}

.camera-start h4 {
  margin-bottom: 0.5rem;
  color: #f1f5f9;
  font-size: 1.5rem;
  font-weight: 600;
}

.camera-start p {
  color: #94a3b8;
  margin-bottom: 2rem;
  font-size: 1rem;
  line-height: 1.6;
}

.debug-status {
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.2);
  border-radius: 8px;
  padding: 0.75rem;
  margin: 1rem 0;
  font-family: monospace;
}

.debug-status small {
  color: #94a3b8;
  font-size: 0.8rem;
  line-height: 1.4;
}

.start-camera-button {
  background: linear-gradient(135deg, #3b82f6, #8b5cf6);
  color: white;
  border: none;
  padding: 1.2rem 2.5rem;
  border-radius: 50px;
  font-size: 1.2rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
  min-width: 200px;
  position: relative;
  overflow: hidden;
}

.start-camera-button:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
}

.start-camera-button:disabled {
  background: linear-gradient(45deg, #6b7280, #9ca3af);
  cursor: not-allowed;
  opacity: 0.7;
  transform: none;
}

.camera-retry-container {
  margin-top: 1rem;
  text-align: center;
}

.retry-camera-button {
  background: linear-gradient(45deg, #10b981, #059669);
  color: white;
  border: none;
  padding: 1rem 2rem;
  border-radius: 50px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(16, 185, 129, 0.4);
}

.retry-camera-button:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(16, 185, 129, 0.6);
}

.retry-camera-button:disabled {
  background: linear-gradient(45deg, #6b7280, #9ca3af);
  cursor: not-allowed;
  opacity: 0.7;
  transform: none;
}

.camera-view {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.camera-video {
  width: 100%;
  max-width: 100%;
  height: auto;
  min-height: 240px;
  border-radius: 10px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  background: #000;
  object-fit: cover;
}

.camera-video.enhanced {
  min-height: 300px;
  border: 2px solid rgba(59, 130, 246, 0.3);
  background: #000;
}

.capture-canvas {
  display: none;
}

.capture-canvas.hidden {
  display: none !important;
}

@media (min-width: 480px) {
  .camera-video {
    min-height: 300px;
    border-radius: 12px;
  }
}

@media (min-width: 640px) {
  .camera-video {
    min-height: 360px;
    max-width: 600px;
    border-radius: 15px;
  }
}

@media (min-width: 768px) {
  .camera-video {
    max-width: 640px;
  }
}

.palm-guide {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  pointer-events: none;
}

.guide-outline {
  width: 120px;
  height: 150px;
  border: 2px dashed #667eea;
  border-radius: 30px 30px 15px 15px;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  padding-bottom: 0.5rem;
  background: rgba(102, 126, 234, 0.1);
  animation: pulse-guide 2s infinite;
}

@media (min-width: 480px) {
  .guide-outline {
    width: 160px;
    height: 200px;
    border: 2.5px dashed #667eea;
    border-radius: 40px 40px 18px 18px;
    padding-bottom: 0.75rem;
  }
}

@media (min-width: 640px) {
  .guide-outline {
    width: 200px;
    height: 250px;
    border: 3px dashed #667eea;
    border-radius: 50px 50px 20px 20px;
    padding-bottom: 1rem;
  }
}

.guide-outline span {
  color: #667eea;
  font-weight: 600;
  font-size: 0.9rem;
  text-shadow: 0 1px 2px rgba(255,255,255,0.8);
}

@keyframes pulse-guide {
  0%, 100% { opacity: 0.6; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.05); }
}

.camera-controls {
  display: flex;
  gap: 1rem;
  margin-top: 1.5rem;
  justify-content: center;
}

.cancel-button {
  background: #6b7280;
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  border-radius: 25px;
  cursor: pointer;
  font-weight: 500;
  transition: all 0.2s;
}

.cancel-button:hover {
  background: #4b5563;
}

.capture-button {
  background: linear-gradient(135deg, #10b981, #059669);
  color: white;
  border: none;
  padding: 1rem 2rem;
  border-radius: 50px;
  cursor: pointer;
  font-weight: 700;
  font-size: 1.1rem;
  transition: all 0.3s ease;
  box-shadow: 0 8px 25px rgba(16, 185, 129, 0.4);
  min-width: 150px;
  position: relative;
  overflow: hidden;
}

.capture-button:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(16, 185, 129, 0.6);
}

.capture-button:disabled {
  background: #9ca3af;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}

.cancel-button:disabled {
  background: #9ca3af;
  cursor: not-allowed;
}

.hidden-canvas {
  display: none;
}

.technical-details {
  font-size: 0.8rem;
  overflow: auto;
  background: #f8f9fa;
  padding: 0.5rem;
  border-radius: 5px;
  border: 1px solid #e9ecef;
}

/* 404 Error Page Styles */
.error-page {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  text-align: center;
  padding: 2rem;
}

.error-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.error-title {
  font-size: 2rem;
  margin-bottom: 1rem;
}

.error-message {
  font-size: 1.2rem;
  margin-bottom: 2rem;
}

.error-button {
  background: rgba(255,255,255,0.2);
  color: white;
  padding: 1rem 2rem;
  border-radius: 50px;
  text-decoration: none;
  font-size: 1.1rem;
  font-weight: 600;
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255,255,255,0.3);
  transition: all 0.3s ease;
}

.error-button:hover {
  background: rgba(255,255,255,0.3);
  transform: translateY(-2px);
}

/* Enhanced Palm Reading Styles */
.reading-content {
  line-height: 1.8;
}

.section-header {
  font-size: 1.5rem;
  font-weight: 700;
  color: #3b82f6;
  margin: 2rem 0 1rem 0;
  padding: 0.5rem 0;
  border-bottom: 2px solid rgba(59, 130, 246, 0.3);
}

.reading-section {
  font-weight: 600;
  color: #8b5cf6;
  margin: 1.5rem 0 0.5rem 0;
  font-size: 1.1rem;
}

.reading-text {
  margin: 0.5rem 0;
  color: #e2e8f0;
  line-height: 1.6;
}

.reading-point {
  margin: 0.5rem 0;
  color: #10b981;
  padding-left: 1rem;
  font-weight: 500;
}

/* New Analysis Components */
.analysis-info {
  margin-top: 1rem;
  padding: 1rem;
  background: rgba(59, 130, 246, 0.1);
  border-radius: 10px;
  border-left: 4px solid #3b82f6;
}

.analysis-info p {
  margin: 0.5rem 0;
  color: #e2e8f0;
  font-size: 0.95rem;
}

.line-breakdown {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin: 1rem 0;
}

.line-stat {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem;
  background: rgba(15, 23, 42, 0.6);
  border-radius: 8px;
}

.line-color {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  display: inline-block;
}

.line-color.heart { background: #ef4444; }
.line-color.head { background: #10b981; }
.line-color.life { background: #3b82f6; }
.line-color.fate { background: #8b5cf6; }
.line-color.minor { background: #6b7280; }

.palm-features {
  margin-top: 1rem;
}

.palm-features summary {
  cursor: pointer;
  font-weight: 600;
  color: #3b82f6;
  padding: 0.5rem;
  border-radius: 5px;
  background: rgba(59, 130, 246, 0.1);
}

.palm-features summary:hover {
  background: rgba(59, 130, 246, 0.2);
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 0.5rem;
  margin-top: 1rem;
  padding: 1rem;
  background: rgba(15, 23, 42, 0.4);
  border-radius: 8px;
}

.features-grid p {
  margin: 0.25rem 0;
  color: #94a3b8;
  font-size: 0.9rem;
}

.predictions-section {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 2rem;
  border-radius: 15px;
  margin: 2rem 0;
}

.predictions-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1.5rem;
  margin-top: 1rem;
}

.prediction-card {
  background: rgba(255, 255, 255, 0.1);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border-radius: 10px;
  padding: 1.5rem;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.prediction-card h4 {
  margin-bottom: 1rem;
  font-size: 1.1rem;
  color: #ffd700;
}

.prediction-card ul {
  list-style: none;
  padding: 0;
}

.prediction-card li {
  padding: 0.5rem 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  font-size: 0.95rem;
}

.prediction-card li:last-child {
  border-bottom: none;
}

.personality-section {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  color: white;
  padding: 2rem;
  border-radius: 15px;
  margin: 2rem 0;
}

.personality-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1.5rem;
  margin-top: 1rem;
}

.personality-card {
  background: rgba(255, 255, 255, 0.15);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  border-radius: 10px;
  padding: 1.5rem;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.personality-card h4 {
  margin-bottom: 1rem;
  color: #fff;
  font-size: 1.1rem;
}

.personality-card ul {
  list-style: none;
  padding: 0;
}

.personality-card li {
  padding: 0.3rem 0;
  font-size: 0.95rem;
  opacity: 0.9;
}

.personality-card p {
  font-size: 1rem;
  line-height: 1.6;
  opacity: 0.95;
}

.compatibility-section {
  background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
  color: #2d3748;
  padding: 2rem;
  border-radius: 15px;
  margin: 2rem 0;
}

.compatibility-section h3 {
  color: white;
  text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
}

.compatibility-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1.5rem;
  margin-top: 1rem;
}

.compatibility-card {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 10px;
  padding: 1.5rem;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
}

.compatibility-card.best {
  border-left: 4px solid #e91e63;
}

.compatibility-card.good {
  border-left: 4px solid #4caf50;
}

.compatibility-card h4 {
  margin-bottom: 1rem;
  color: #2d3748;
}

.compatibility-card ul {
  list-style: none;
  padding: 0;
}

.compatibility-card li {
  padding: 0.5rem 0;
  border-bottom: 1px solid #e2e8f0;
  font-size: 0.95rem;
  color: #4a5568;
}

.compatibility-card li:last-child {
  border-bottom: none;
}

@media (max-width: 768px) {
  .predictions-grid,
  .personality-grid,
  .compatibility-grid {
    grid-template-columns: 1fr;
  }
  
  .prediction-card,
  .personality-card,
  .compatibility-card {
    padding: 1rem;
  }
}

/* Responsive Design */
@media (max-width: 768px) {
  .container {
    padding: 1rem;
  }
  
  .hero h1 {
    font-size: 2.5rem;
  }
  
  .hero p {
    font-size: 1.1rem;
  }
  
  .upload-area {
    padding: 2rem 1rem;
  }
  
  .upload-buttons {
    flex-direction: column;
  }
  
  .preview-section {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  
  .predictions-grid,
  .personality-grid,
  .compatibility-grid {
    grid-template-columns: 1fr;
  }
  
  .prediction-card,
  .personality-card,
  .compatibility-card {
    padding: 1.5rem;
  }
  
  .camera-modal {
    padding: 1rem;
    margin: 1rem;
  }
  
  .camera-controls {
    flex-direction: column;
  }
}

/* Extra small screens - ensure all 4 feature cards show */
@media (max-width: 480px) {
  .hero h1 {
    font-size: 2rem;
  }
  
  .upload-area {
    padding: 1.5rem 1rem;
  }
  
  .upload-content h3 {
    font-size: 1.4rem;
  }
  
  /* Force display of all feature cards on very small screens */
  .features {
    display: grid !important;
    grid-template-columns: 1fr !important;
    gap: 1.25rem !important;
    margin: 1.5rem 0 !important;
    padding: 0 0.25rem !important;
  }
  
  .feature-card {
    padding: 1.25rem !important;
    margin-bottom: 0.5rem !important;
    min-height: 160px !important;
    border-radius: 12px !important;
  }
  
  .feature-card h3 {
    font-size: 1.1rem !important;
    margin-bottom: 0.5rem !important;
  }
  
  .feature-card p {
    font-size: 0.9rem !important;
    line-height: 1.4 !important;
  }
  
  .feature-icon {
    font-size: 2rem !important;
    margin-bottom: 0.5rem !important;
  }
  
  .predictions-section,
  .personality-section,
  .compatibility-section {
    padding: 1.5rem;
  }
  
  .predictions-section h3,
  .personality-section h3,
  .compatibility-section h3 {
    font-size: 1.5rem;
  }
}

/* Mobile-Specific Optimizations */
@media (max-width: 767px) {
  /* Touch-friendly buttons */
  button, .upload-button, .camera-button {
    min-height: 44px;
    min-width: 44px;
  }
  
  /* Prevent horizontal scroll */
  body {
    overflow-x: hidden;
  }
  
  /* Better text rendering on mobile */
  body {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }
  
  /* Improve tap targets */
  .feature-card, .upload-area {
    cursor: pointer;
    -webkit-tap-highlight-color: rgba(59, 130, 246, 0.2);
  }
  
  /* Mobile-optimized spacing */
  .hero p {
    font-size: 1rem;
    line-height: 1.6;
    padding: 0 0.5rem;
  }
  
  /* Ensure all 4 feature cards are visible on mobile */
  .features {
    display: grid !important;
    grid-template-columns: 1fr !important;
    gap: 1.5rem !important;
    margin: 2rem 0 !important;
    padding: 0 0.5rem !important;
    width: 100% !important;
    max-width: 100% !important;
  }
  
  .feature-card {
    display: flex !important;
    flex-direction: column !important;
    width: 100% !important;
    max-width: 100% !important;
    margin-bottom: 1rem !important;
    visibility: visible !important;
    opacity: 1 !important;
    position: relative !important;
    background: rgba(15, 23, 42, 0.9) !important;
    min-height: 180px !important;
  }
  
  /* Ensure readable font sizes */
  .feature-card p {
    font-size: 0.95rem;
    line-height: 1.5;
  }
  
  /* Loading spinner mobile optimization */
  .loading-container {
    padding: 1rem;
  }
  
  /* Results mobile formatting */
  .results-container {
    padding: 1rem;
    margin: 1rem 0;
  }
}

/* Touch device optimizations */
@media (hover: none) and (pointer: coarse) {
  .feature-card:hover {
    transform: none;
  }
  
  .upload-button:hover, .camera-button:hover {
    transform: none;
    box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
  }
}

/* Landscape mobile optimization */
@media screen and (max-height: 500px) and (orientation: landscape) {
  .hero {
    padding: 1rem 0;
    margin-bottom: 1rem;
  }
  
  .hero h1 {
    font-size: 1.75rem;
  }
  
  .camera-modal {
    max-height: 98vh;
    padding: 0.5rem;
  }
  
  .camera-video {
    min-height: 200px;
  }
}

/* High DPI displays */
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
  .feature-card, .upload-area, .camera-modal {
    border-width: 0.5px;
  }
}

/* Debug: Force all feature cards to be visible on mobile */
@media (max-width: 640px) {
  .features {
    display: grid !important;
    grid-template-columns: 1fr !important;
    grid-template-rows: repeat(4, auto) !important;
    gap: 1.5rem !important;
    width: 100% !important;
    margin: 2rem auto !important;
    padding: 0 1rem !important;
    box-sizing: border-box !important;
  }
  
  .feature-card:nth-child(1),
  .feature-card:nth-child(2),
  .feature-card:nth-child(3),
  .feature-card:nth-child(4) {
    display: flex !important;
    visibility: visible !important;
    opacity: 1 !important;
    position: static !important;
    transform: none !important;
    width: 100% !important;
    max-width: 100% !important;
    height: auto !important;
    min-height: 180px !important;
    margin: 0 0 1rem 0 !important;
    grid-column: 1 !important;
  }
}

/* Ultra-safe mobile display rules */
@media screen and (max-width: 479px) {
  .features {
    display: block !important;
    width: 100% !important;
    padding: 0 0.5rem !important;
  }
  
  .feature-card {
    display: block !important;
    width: 100% !important;
    margin-bottom: 1.5rem !important;
    float: none !important;
    position: static !important;
    clear: both !important;
  }
}

/* Emergency fallback - ensure all cards are always visible */
.feature-card:nth-child(1),
.feature-card:nth-child(2),
.feature-card:nth-child(3),
.feature-card:nth-child(4) {
  display: block !important;
  visibility: visible !important;
  opacity: 1 !important;
}

/* Mobile viewport height fix */
@media screen and (max-height: 600px) and (orientation: portrait) {
  .features {
    margin: 1rem 0 !important;
  }
  
  .feature-card {
    min-height: 150px !important;
    padding: 1rem !important;
  }
}

